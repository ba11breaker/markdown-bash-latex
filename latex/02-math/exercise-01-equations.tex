% =============================================================================
% Exercise 1: Mathematical Equations
% =============================================================================
% Reproduce each equation described in the TODOs.
% =============================================================================

\documentclass[12pt,a4paper]{article}
\usepackage[utf8]{inputenc}
\usepackage{amsmath}
\usepackage{amssymb}

\title{Math Exercises}
\author{Student}
\date{\today}

\begin{document}
\maketitle

\section{Inline Math}

% TODO 1: Write a paragraph that includes these inline expressions:
% "The area of a circle is A = pi*r^2, where r is the radius."
% "Einstein showed that E = mc^2."
% "For any right triangle, a^2 + b^2 = c^2."


\section{Fractions and Roots}

% TODO 2: Write these as display math:
% a) The quadratic formula: x = (-b +/- sqrt(b^2 - 4ac)) / 2a
% b) A continued fraction: 1 + 1/(1 + 1/(1 + 1/...))
% c) sqrt(sqrt(x)) (nested square roots)


\section{Sums and Integrals}

% TODO 3: Write these as display math:
% a) Sum from i=1 to n of i^2
% b) Integral from 0 to infinity of e^(-x) dx
% c) Product from k=1 to n of (1 + x_k)
% d) Limit as x approaches 0 of sin(x)/x = 1


\section{Matrices}

% TODO 4: Write these matrices:
% a) A 2x2 identity matrix with round brackets
% b) A 3x3 matrix with square brackets:
%    | 1  2  3 |
%    | 4  5  6 |
%    | 7  8  9 |
% c) A determinant (vertical bars):
%    | a  b |
%    | c  d |  = ad - bc


\section{Aligned Equations}

% TODO 5: Write the following aligned derivation:
%   (x + y)^2 = (x + y)(x + y)
%             = x^2 + xy + yx + y^2
%             = x^2 + 2xy + y^2


\section{Piecewise Function}

% TODO 6: Write the absolute value function:
%   |x| = { x    if x >= 0
%          { -x   if x < 0


\section{Greek Letters}

% TODO 7: Write the following formula using Greek letters:
% "The standard normal distribution PDF is:
%  phi(x) = (1 / sqrt(2*pi*sigma^2)) * e^(-(x-mu)^2 / (2*sigma^2))"


\section{Challenge: Maxwell's Equations}

% TODO 8 (Bonus): Write Maxwell's equations in differential form:
% nabla . E = rho / epsilon_0
% nabla . B = 0
% nabla x E = -dB/dt
% nabla x B = mu_0 * J + mu_0 * epsilon_0 * dE/dt


\end{document}
